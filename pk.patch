# Make a local copy of the public key to pass the SUPERCOP test that overlaps public key and opened message
# patch crypto_sign/crossrsdp128balanced/clean/sign.c < pk.patch
# patch crypto_sign/crossrsdp128balanced/avx2/sign.c < pk.patch
83c83
<                      const unsigned char *pk)                   // in parameter
---
>                      const unsigned char *pk_static)            // in parameter
85a86,89
>    /* SUPERCOP edit: make a local copy of the public key */
>    const pk_t pk;
>    memcpy((unsigned char *) &pk, pk_static, sizeof(pk_t));
> 
90c94
<    int ok = CROSS_verify((const pk_t *const) pk,            // in parameter
---
>    int ok = CROSS_verify(&pk,                                   // in parameter
